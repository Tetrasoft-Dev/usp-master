# --- SEGURANÇA E JWT (Definições Locais Padrão) ---
# Altere em produção. O segredo deve ter pelo menos 32 caracteres.
JWT_SECRET=rDOQoFCGX1yH5upVjzewKvI9c64S2EdYlRTBbhZUqWsLMxft3n780gkPaimJAN

# --- CONFIGURAÇÃO DA BASE DE DADOS ---
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# --- PORTAS DOS SERVIÇOS ---
KONG_PORT=8000
STUDIO_PORT=3000

#./mvnw clean spring-boot:run

#-- 1. Cria a função que copia o utilizador novo para a nossa tabela de perfis
#CREATE OR REPLACE FUNCTION public.handle_new_user()
#RETURNS TRIGGER AS $$
#BEGIN
#    INSERT INTO public.perfis (id, nome, perfil)
#    VALUES (new.id, split_part(new.email, '@', 1), 'ROLE_USER'::user_role)
#    ON CONFLICT (id) DO NOTHING;
#    RETURN NEW;
#END;
#$$ LANGUAGE plpgsql SECURITY DEFINER;

#-- 2. Liga esta função ao evento de criação de utilizadores do Supabase
#DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
#CREATE TRIGGER on_auth_user_created
#    AFTER INSERT ON auth.users
#    FOR EACH ROW EXECUTE FUNCTION public.handle_new_user();