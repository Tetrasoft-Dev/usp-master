services:
  # 1. Base de Dados PostgreSQL (Atualizada para a versão estável compatível)
  supabase-db:
    image: supabase/postgres:15.6.1.138
    container_name: supabase-db
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - supabase_db_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres", "-d", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # 2. Postgres Meta (Atualizado)
  supabase-meta:
    image: supabase/postgres-meta:v0.84.2
    container_name: supabase-meta
    depends_on:
      supabase-db:
        condition: service_healthy
    environment:
      PG_META_PORT: 8080
      PG_META_DB_HOST: supabase-db
      PG_META_DB_NAME: postgres
      PG_META_DB_USER: postgres
      PG_META_DB_PASSWORD: postgres
      PG_META_DB_PORT: 5432
    ports:
      - "8085:8080"

  # 3. Serviço de Autenticação (GoTrue - Versão sincronizada com o banco)
  supabase-auth:
    image: supabase/gotrue:v2.143.0
    container_name: supabase-auth
    depends_on:
      supabase-db:
        condition: service_healthy
    command: ["auth"]
    environment:
      GOTRUE_API_HOST: 0.0.0.0
      GOTRUE_API_PORT: 9999
      GOTRUE_API_EXTERNAL_URL: http://localhost:9999
      API_EXTERNAL_URL: http://localhost:9999
      GOTRUE_DB_DRIVER: postgres
      GOTRUE_DB_DATABASE_URL: postgres://postgres:postgres@supabase-db:5432/postgres?sslmode=disable
      GOTRUE_DB_SEARCH_PATH: auth,public
      GOTRUE_JWT_SECRET: ${JWT_SECRET}
      GOTRUE_JWT_EXP: 3600
      GOTRUE_SITE_URL: http://localhost:8080
      GOTRUE_DISABLE_SIGNUPS: "false"
      GOTRUE_EXTERNAL_EMAIL_ENABLED: "true"
      GOTRUE_MAILER_AUTOCONFIRM: "true"
      GOTRUE_SMS_AUTOCONFIRM: "true"
      GOTRUE_SMTP_ADMIN_EMAIL: admin@localhost.com
      GOTRUE_SMTP_HOST: localhost
      GOTRUE_SMTP_PORT: 1025
      GOTRUE_SMTP_USER: fake_user
      GOTRUE_SMTP_PASS: fake_password
    ports:
      - "9999:9999"

  # 4. Painel Web de Gestão (Supabase Studio)
  supabase-studio:
    image: supabase/studio:latest
    container_name: supabase-studio
    depends_on:
      supabase-db:
        condition: service_healthy
      supabase-meta:
        condition: service_started
    environment:
      STUDIO_DEFAULT_ORGANIZATION: "Local Dev"
      STUDIO_DEFAULT_PROJECT: "Projeto Spring"
      POSTGRES_PASSWORD: postgres
      DATABASE_URL: postgres://postgres:postgres@supabase-db:5432/postgres
      SUPABASE_PUBLIC_URL: https://github.dev
      POSTGRES_HOST: supabase-db
      POSTGRES_PORT: 5432
      STUDIO_PG_META_URL: http://supabase-meta:8080
    ports:
      - "${STUDIO_PORT}:3000"

volumes:
  supabase_db_data:
    driver: local
